"use client";

import React from 'react';

const HeroCarousel: React.FC = () => {
  return (
    <section className="w-full h-[400px] bg-gray-200 flex items-center justify-center text-gray-600">
      <p className="text-2xl font-semibold">Hero Carousel Placeholder</p>
      {/* Questo è un placeholder. Qui andrebbe il codice per un carosello di immagini. */}
    </section>
  );
};

export default HeroCarousel;